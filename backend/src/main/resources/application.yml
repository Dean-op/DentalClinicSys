server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dental_clinic?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 123456
  sql:
    init:
      mode: always
      encoding: UTF-8
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  jwt-secret: "dental-clinic-demo-secret-change-before-production"
  upload-dir: uploads
  ai:
    # Real base_url/api_key/model are stored in local MySQL table ai_config.
    # Do not commit real API keys to source control.
    timeout-seconds: 45
